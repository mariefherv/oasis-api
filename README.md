***Install the necessary packages through this command:***

```
npm install --save-exact
```

***To run the API:***
```
npm run dev
```
>Note: Make sure that there isn't anything running on the default port (4000). If there is, you may change the port on the ENV file.
